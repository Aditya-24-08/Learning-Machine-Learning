This folder not only fetches data from .tsv file but also assigns
labels to it using target numbers in the last column of the data
& also plots various 2D plots between features of this dataset.
2D-plots both before and after normalization using Z-scores
have been plotted using matplotlib.pyplot functions.