Each source code written is python. To run each file, one just needs to call the main function which takes on the entire program sequence by itself
while at the same time describing the results too.
For example: to run part_a.py

from part_a import *
main()
